- name: test
  hosts: "{{ VM  }}"
  become: false
  gather_facts: no
  tasks:
    
    - name: Creation de l entree dans le DNS
      win_dns_record:
        name: "{{ nomDNS }}"
        type: "A"
        value: "{{ ipDNS }}"
        zone: "bsclab.local"
        state: present
        computer_name: srvAD1
