- name: Deployment of the new VM named "{{ VMname }}"
  hosts: localhost
  gather_facts: no
  become: no

  tasks:
    
    - name: Build a list of all the folders with the type VIRTUAL_MACHINE and called vm
      vmware.vmware_rest.vcenter_folder_info:
        filter_type: VIRTUAL_MACHINE
      register: my_folders

    - name: show
      ansible.builtin.debug:
        var: my_folders

    - name: create the VM
      vmware.vmware_rest.vcenter_vm:
        vcenter_hostname: "dcavcsa1a.dca.scc"
        vcenter_username: "tgrevellec"
        vcenter_password: 'Tb"%c[[V@7__=.Neu\\y'
        vcenter_validate_certs: False
        name: "{{ VMname }}"
        placement:
          folder: "{{ lookup('vmware.vmware_rest.folder_moid', '/DCA/Environnement/tgrevellec') }}"
          datacenter: "DCA"
        state: present
        power_on: true