- hosts: localhost
  gather_facts: no
  connection: local
  tasks:
    - name: Changement du prompt
      shell: "ls -l /etc/"
      register: resul

    - name: Affichage de la VAR
      debug:
        msg: "{{ resul }}"
