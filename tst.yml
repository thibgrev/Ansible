- name: test
  hosts: "{{ nomVM }}"
  gather_facts: no
  tasks:
    - name: Change the hostname of the new VM with his real name "{{ VMname }}"
      hostname:
        name: "{{ nouveauNomVM  }}"
        use: systemd

    - name: reboot
      reboot:
